FROM node:20-alpine
WORKDIR /app

# Install deps at build time — node_modules baked into image
COPY package.json package-lock.json ./
RUN npm ci --prefer-offline

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
